package variables;

public class OperacionesConVariables {

    // OPERADORES DE COMPARACIÓN
    int numeroUno = 5;
    int numeroDos = 9;
    boolean resultado = numeroUno == numeroDos;  // == es igual a
    // != (distinto a), < (menor a), > (mayor a), >= (mayor o igual a), <= (menor o igual a)

    // OPERADORES ARITMÉTICOS
    String caracterUno = "hola ";
    String caracterDos = "mundo";
    String resultado2 = caracterUno + caracterDos; // -> resultado "hola mundo"
    // + (suma, concatena), - (resta), * (multiplicación), / (división), % (resto de una división), ++ (incrementar), -- (decrementar)

    // OPERADORES DE ASIGNACIÓN
    // =, +=, -=, *=, /=, %=, (desplazamiento bit a bit-->>), &=, |=, ^=, >>=, <<=

    // OPERADORES LÓGICOS
    // && (AND ó Y), || (OR ó O), ! (NOT ó negación)
}
